import { SignedOut } from "@clerk/nextjs";
import React from "react";

type Props = {};

const SignedOutButton = (props: Props) => {
  return <SignedOut />;
};

export default SignedOutButton;
